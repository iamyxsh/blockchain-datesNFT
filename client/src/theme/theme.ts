import { createTheme } from "@material-ui/core/styles"

const theme = createTheme({
	palette: {
		primary: {
			main: "#EB92BE",
		},
		secondary: {
			main: "#B1FFFD",
		},
	},
	typography: {
		fontFamily: "Roboto",
	},
})

export default theme
